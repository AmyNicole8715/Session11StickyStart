@model IEnumerable<stickynotes.Models.Sticky>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<body>
<div>
	<form asp-action="Create"  >

	<textarea name="content" id="content"></textarea>
    @* <input type="hidden" name="authenticity_token" id="auth_tok" value="@"> *@
    
	@* <input type="color" id="colorPicker" value="#fdff97" onblur="updatePaper()">
	<div id="paper" class="paper"></div> *@
	
	<button type="submit" id="ButtonID">Post!</button></form>
	
</div>




<p>
    <a asp-action="Create">Create New</a>
</p>

@foreach (var item in Model) {
        
        <div>
            <div class="note paper">
                @Html.DisplayFor(modelItem => item.Content)
                
            </div>
            <div>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </div>
        </div>
        
}
    </tbody>
</table>
</body>
